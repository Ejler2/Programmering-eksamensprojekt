var resultatNr = 0;
var resultatFarve = "ingen";
var forsøgNr = 1;


function Givresultat (vinkel) {
    if (-3.6 < vinkel && vinkel < -3.6 + 360/37) {
        resultatNr = 11;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37) < vinkel && vinkel < -3.6 + (360/37)*2) {
        resultatNr = 30;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*2 < vinkel && vinkel < -3.6 + (360/37)*3) {
        resultatNr = 8;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*3 < vinkel && vinkel < -3.6 + (360/37)*4) {
        resultatNr = 23;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*4 < vinkel && vinkel < -3.6 + (360/37)*5) {
        resultatNr = 10;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*5 < vinkel && vinkel < -3.6 + (360/37)*6) {
        resultatNr = 5;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*6 < vinkel && vinkel < -3.6 + (360/37)*7) {
        resultatNr = 24;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*7 < vinkel && vinkel < -3.6 + (360/37)*8) {
        resultatNr = 16;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*8 < vinkel && vinkel < -3.6 + (360/37)*9) {
        resultatNr = 33;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*9 < vinkel && vinkel < -3.6 + (360/37)*10) {
        resultatNr = 1;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*10 < vinkel && vinkel < -3.6 + (360/37)*11) {
        resultatNr = 20;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*11 < vinkel && vinkel < -3.6 + (360/37)*12) {
        resultatNr = 14;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*12 < vinkel && vinkel < -3.6 + (360/37)*13) {
        resultatNr = 31;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*13 < vinkel && vinkel < -3.6 + (360/37)*14) {
        resultatNr = 9;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*14 < vinkel && vinkel < -3.6 + (360/37)*15) {
        resultatNr = 22;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*15 < vinkel && vinkel < -3.6 + (360/37)*16) {
        resultatNr = 18;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*16 < vinkel && vinkel < -3.6 + (360/37)*17) {
        resultatNr = 29;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*17 < vinkel && vinkel < -3.6 + (360/37)*18) {
        resultatNr = 7;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*18 < vinkel && vinkel < -3.6 + (360/37)*19) {
        resultatNr = 28;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*19 < vinkel && vinkel < -3.6 + (360/37)*20) {
        resultatNr = 12;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*20 < vinkel && vinkel < -3.6 + (360/37)*21) {
        resultatNr = 35;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*21 < vinkel && vinkel < -3.6 + (360/37)*22) {
        resultatNr = 3;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*22 < vinkel && vinkel < -3.6 + (360/37)*23) {
        resultatNr = 26;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*23 < vinkel && vinkel < -3.6 + (360/37)*24) {
        resultatNr = 0;
        resultatFarve = "grøn";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*24 < vinkel && vinkel < -3.6 + (360/37)*25) {
        resultatNr = 32;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*25 < vinkel && vinkel < -3.6 + (360/37)*26) {
        resultatNr = 15;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*26 < vinkel && vinkel < -3.6 + (360/37)*27) {
        resultatNr = 19;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*27 < vinkel && vinkel < -3.6 + (360/37)*28) {
        resultatNr = 4;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*28 < vinkel && vinkel < -3.6 + (360/37)*29) {
        resultatNr = 21;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*29 < vinkel && vinkel < -3.6 + (360/37)*30) {
        resultatNr = 2;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*30 < vinkel && vinkel < -3.6 + (360/37)*31) {
        resultatNr = 25;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*31 < vinkel && vinkel < -3.6 + (360/37)*32) {
        resultatNr = 17;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*32 < vinkel && vinkel < -3.6 + (360/37)*33) {
        resultatNr = 34;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*33 < vinkel && vinkel < -3.6 + (360/37)*34) {
        resultatNr = 6;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*34 < vinkel && vinkel < -3.6 + (360/37)*35) {
        resultatNr = 27;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*35 < vinkel && vinkel < -3.6 + (360/37)*36) {
        resultatNr = 13;
        resultatFarve = "sort";
        console.log(resultatFarve + " " + resultatNr);
    } else if (-3.6 + (360/37)*36 < vinkel && vinkel < -3.6 + (360/37)*37) {
        resultatNr = 36;
        resultatFarve = "rød";
        console.log(resultatFarve + " " + resultatNr);
    } else {
        console.log("Der er sket en fejl!")
    }
}